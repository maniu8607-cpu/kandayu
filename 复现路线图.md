# 砍大鱼经营 · 复现路线图

> B线路线图 + 跨会话进度快照。规格来源：`../参考解包_index3/复现规格_总纲.md` + `代码行为规格.md` + 《砍大鱼经营 Playable脚本.md》。
> 更新日期：2026-07-30

## 进度快照（回来从这里接）

**第二十七轮（首屏逐帧对齐竞品：相机构图 + 五项表现）：**
- ~~**相机构图根修**（用户点名「全屏能看到地图边界」）：实测地编机位瞄点落在主角东北 11u 的河面((10,-10) vs 主角(0.5,-4))，主角被压画面角落+露地图外灰边。探针里逐组试出对齐竞品的构图：Game.cameraOffset (-11,24,8)→**(-17,24,11)**、camZoomOut 1.25→**0.85**（MCP 已写场景值）。这俩 start() 一次性采样，运行中在 Inspector 改不生效，重开预览才见效~~
- ~~**引导大箭头隐形根因**：场景「大箭头」节点是**空节点**（无渲染组件无子级），GuideArrow 只在属性留空时才自动建画面。改为「无 MeshRenderer 就补画面」+ 竖立 billboard 朝相机 + 新契约图 arrow_big（绿色下指箭头，竞品同款）。**坑：竖立 quad 的贴图 V 轴与 PIL 画布上下颠倒**，要下指就按上指画（groundQuad 平放的不受影响，arrow_tri 原方向是对的）~~
- ~~**地贴占位图全换**：4 底框（米色亮贴/绿色踩亮）+6 图标（木板/脚印/火焰/铜钱/切刀/扩建）PIL 4x 超采样程序绘制，对齐竞品米色亮贴观感。正式美术仍可同名覆盖（契约不变）~~
- ~~**拖动提示钉主角脚下**（竞品样式）：Game.updateDragTipFollow 每帧 convertToUINode 投影；旋钮 dragTipFollow（关=回屏幕固定位）/dragTipFollowOffset；跟随模式下摘掉 DragTip 的 Widget（重激活会拉回对齐位打架）~~
- ~~**左上常驻 logo+Play now**：HUD.buildBrand（Widget 贴左上、六语图、按钮 1s 脉冲、点击 [sdk] jump store、结算页弹出自动收起）~~
- ~~**鱼游动泡圈**（竞品鱼嘴前白圈串）：Fish.spawnBubble 6 节点池循环复用（不逐泡新建材质），契约图 fish_bubble，旋钮 bubbleInterval/bubbleAhead，嘴前 2.4u 随机散布、涨大缩没~~
- **待美术**：六语 logo 图是错的（zh/logo.png 内容是「三国冰河时代」，别的游戏的图）——正确 logo 同名覆盖 `resources/texture/ui/{en,ja,ko,vi,zh,zh-tw}/logo.png`
- 遗留观察：groundQuad 贴图异步首载的一瞬会闪品红（mesh 无材质），稳态无此现象
- 首屏已确认与竞品一致：构图/亮贴/大箭头+箭头链/脚下摇杆提示/左上导流位/鱼泡圈；差异保留项不变（铜钱、踩贴显桩、中文提示）

**第二十六轮（git 版本化 + 场景层级重构 + 数值调优面板）：**
- **git 已建立**：远程 https://github.com/maniu8607-cpu/kandayu ；基线 `2688e95`、重构 `11be772`。以后每轮改动前后 commit+push
- **场景全中文规范命名**（uuid 不变，连线全保持）：GameRoot 下 主角/锚点组(鱼_出生点…)/引导地贴组(贴01_放木桩…贴12_砍鳄鱼)/鱼道_北河/鱼道_鳄鱼二期/烤炉流水线(待烤盘/炉膛/烤后盘/售卖盘)/金币盘/顾客队列/顾客模板/帮工/引导箭头/飞行道具层；新容器 Map_1/玩法设施(烤炉_主区/烤炉_扩建区/传送带/切割机/拦鱼木桩 + 围墙_打通拆除[WeiQiang_9-12])；功能桌改名 桌子_售卖窗/烤后区/扩建区AB；_废弃_CutterAnim 待用户编辑器删
- **⚠️ 探针路径变更**：`GameRoot/Player`→`GameRoot/主角`，其余同理按新名
- **数值调优面板**：Game 组件 Inspector「数值调优」组 11 项（0=默认）：鱼速/鱼血/砍鱼间隔/两种伤害/两种掉肉数/传送带间隔/背包与地面上限。掉肉数已配置化（GameConfig.MEAT_PER_CHOP/CUTTER_MEAT）
- **重构工艺**（防复发）：FBX prefab 实例的 `_name` 不在节点对象上，在 `PropertyOverrideInfo(propertyPath=['_name'])` 里——按 cc.Node._name 匹配会漏掉全部 FBX 实例；重名实例用 _lpos override 消歧；改前必须 Cocos 完全关闭+备份 .bak
- 代码兼容：FishBlock/鱼_拦停参考点 双名、拆墙 walk 兼容 围墙_打通拆除 容器与旧散名

**第二十五轮（竞品录屏对比 + 全修四项）：**
- **拿到竞品真实运行画面**（试玩录屏.mp4 = index(3).html「PA砍大鱼快速迭代2D / TILES SURVIVE!」109s 全流程），抽帧对比分析产出差距清单（抽帧在 scratchpad/vid/）
- ~~**切割机切鱼持续染红+血泊**（竞品最强视觉反馈）：Fish.hit 加 sustainRed 参数（切割机砍传 true），鱼保持红 tint 不恢复；鱼身下铺血泊面片（契约图 `blood_pool` 缺省暗红块，死后残留 2s）~~
- ~~**烤后桌白盘出餐感**：熟肉垫程序化白圆盘（cylinder），两摞沿桌长边叠高（竞品白盘摞天际线）。坑×2：primitives.cylinder 第三参是 opts 对象（传数字→高度默认 2 变白柱）；盘径要大于肉排裙边才露白边~~
- ~~鱼密度：respawnDistPx 300（同屏 2-3 条）~~
- ~~相机视野拉远：Game.camZoomOut=1.25（旋钮，1=地编机位原样）~~
- 录屏确认已一致：木桩拦鱼/掉肉散落/箭头链/大箭头/光环/顾客气泡/解锁数字牌/传送带/帮手/鳄鱼/结算页。按用户拍板保留差异：扔木头→木桩显现、绿钞→金色铜钱、2D→3D
- 透视教训：叠高物在斜相机下视觉漂移,判定「挂点悬空」前先量局部/世界坐标（backPan 实际正在桌上）

**第二十四轮（正确理解「栅栏」后的区域重修）：**
- **地图区域正解**（此前理解错了两轮）：`WeiQiang_9/10/11/12` 组成沿 **z=1.7 横贯作业区的中央围墙**——北=主玩法区（砍鱼/烤炉/售卖），南=扩建区。「打通壁垒」拆的就是这道墙
- ~~主角南边界封到中央墙内侧：boundMaxZ=1.1（之前 5.7 是封外圈南栅栏，中央墙照穿）；onExpand 放开到 19~~
- ~~onExpand 拆墙：把 z≈1.7 的 WeiQiang_9/10/11/12 压扁隐藏（fenceNode 逻辑保留兼容）~~
- ~~扩建贴从墙南 (-1,5.5)（主角够不到的错误一侧）移到墙北 (-1,0.7)~~
- ~~帮工出生位移到 (-3.2,-0.8)（远离中央墙，贴近售卖窗工作起点）~~
- ~~血条底框黑→白：底框改契约图 `hpbar_frame`（暂缺回退白色块），血量条=红色条贴图~~
- ~~MAX 提示被背包肉柱埋住（不是图反了）：高度动态=堆顶+0.6，永远浮在堆叠物上方~~
- ~~掉肉改竞品式随机环带散布（面积均匀、椭圆压扁、平铺地面随机朝向），不再固定槽位码堆;收进背包才是整齐单列~~
- **待美术**：`hpbar_frame.png`（白色圆角条,放 texture/ui/ 即替换白色块）

**第二十三轮（血条/烤炉/肉色/散布/帮工/区域 七项）：**
- ~~**黑血条根因=两张图角色用反**：`hpbar_fill.png` 其实是黑色条(该做底槽)、`hpbar_bg.png` 是红色条(该做血量)，文件名语义与内容相反。代码已交换：黑条为底、红条为血~~
- ~~烤炉回一次一片（用户拍板，cookBatch=1，场景值已改；旋钮保留）~~
- ~~**白肉根因=材质异步首载**：Skin.apply 第一次加载贴图期间掉的肉保持 FBX 白模（之后缓存同步就是粉色，所以截图验证一直看不出）。Game.start 预热 T_yu_BC/jinibi_2 材质~~
- ~~掉肉错落有致：固定槽位基础上加 ±0.36 随机散布 + 随机朝向~~
- ~~帮工单趟搬 10 块（helperCarryMax 旋钮）：后桌拿最多 10 块顶头上叠一摞→售卖盘逐块放~~
- ~~**南栅栏缝隙**：南栅栏 z≈6.3 与南河阻挡 z=7.0 之间有缝，主角能穿栅栏站缝里。boundMaxZ=5.7 封死；onExpand（打通壁垒）时放开到 19 进南区~~
- ~~帮工出生位从 (-4.5,0.5)（西栅栏外）移进 (-3.2,0.5)~~
- 区域核对：掉肉/拾肉 (1.8,-4.6)、切割机 (2.7,-5.5)、传送带 (2.8,-4.0)、扩建贴 (-1,5.5 栅栏内侧)、chop2 (3,6.2 拆墙后可达) 均在位
- MCP CID 备忘：ProcessLine=cc4bdJFf/tOHrbKFw8gvGoq @节点 21+y9gpcNHMooIezrLJdDt

**第二十二轮（布局与手感四项）：**
- ~~金币盘+收币贴移到卖鱼窗口**北侧** (-3,-3.5)（对照竞品锚点 dump：coinPan 在贩卖点上方；原来在南侧放反了）~~
- ~~主角东侧边界收到栅栏内：PlayerController.boundMaxX 23→4.2（栅栏实测 x=4.7）。实测向东推死停在 4.20~~
- ~~烤后桌摆盘：三列从沿桌子短边(x)改沿长边(z)——原来第二三列悬空伸出桌面，就是「竖着摆」的观感~~
- ~~主角移速减半：Player 的 moveSpeedPx=200（场景值；0=回默认 400）~~
- MCP 经验：set_component_property 报错时会列出节点全部组件 CID，逐个试错即可定位无 CID 记录的组件（PlayerController=af1cbKLbiNJ9p5LPZMlwY4S）

**第二十一轮（七项表现修正）：**
- ~~**鱼双状态模型**（用户指出的正解）：HeTun_Skin01=睁眼正常版、Skin02=X眼晕版是两个状态模型。游动/排队用 01，撞桩瞬间 Animator.swapSkin 热切 02（clips 注册表复用）。FishLane 新增 stunSkinPrefab 槽（场景已连 Skin02），鱼皮三槽全改 01。验证：游动鱼睁眼、拦停鱼晕版+stun 循环，同屏两状态~~
- ~~**挥刀看不见**：俯视角下斗笠盖住手部动作——播骨骼挥刀的同时叠加外层整体前倾 26°（tween ScaleWrap，与骨骼动画不冲突）~~
- ~~**切割机上下砍**：squash 压扁改为整机 y 下压 0.45 回弹（每刀）~~
- ~~**背包满提示**：主角头顶 billboard 气泡（契约图 `texture/ui/tip_small`，缺图红块），满时呼吸缩放，卸货自动消失~~
- ~~**摇杆提示太大**：dragTip 整体 0.62 缩放~~
- ~~**引导箭头链空转**：count 公式多减 1，目标近时整条链为 0 枚（开局引导形同虚设）→去掉 -1、起始距离 70→45px、箭头 0.45→0.7u、y 抬 0.12 防 z-fight；大箭头 1.3 加大、悬浮高度 130→55px 贴近目标~~
- 帽子遮挡的根本盘（记录给用户）：相机俯角较陡（offset y=48），要整体看到人物侧面需调 Light/Main Camera 机位或 Game.cameraOffset
- 遗留：`tip_small` 若不是「背包满」语义的图，美术出一张对应图同名覆盖

**第二十轮（速度旋钮 + 动画资产审计）：**
- 主角移速旋钮：`PlayerController.moveSpeedPx`（Inspector，0=用 GameConfig.PLAYER_SPEED_PX=400 竞品原值），独立于帮工
- 动画连线全景（运行时 dump 实测）：渔夫_Skin+Idle/Run/Attack→主角；阿牛_Skin→顾客模板；大聪明_Skin→帮工；HeTun_Skin02+run/膨胀/晕/die→北河鱼道；EYu_Skin+run→二期鳄鱼道
- **未使用 8 个**：灯笼鱼全套×3、鲨鱼全套×3、HeTun_Skin01——都是策划案「3 鱼版本」备用，不在 resources/ 不进包，无体积浪费，保留
- **发现**：`刀_切割` clip 连在废弃的 CutterAnim 空容器上（无 skinPrefab，从未实际播放）；它是人形挥刀动作，候选用途=主角砍鱼动作（现用 Attack），待用户拍板

**第十九轮（六项体验修正）：**
- ~~**切割机不自动的根因**：updateCutter 和 updateChop 共用 `_chopTimer`，而主角不站砍鱼贴时 updateChop 每帧把它清零——切割机只在主角也站着砍时才动。改独立 `_cutterTimer`。验证：主角站远处，鱼血自动掉、肉自动堆~~
- ~~**撞晕表现**：眩晕星粒子 y=1.8 埋在鱼体内（鱼背高 ~3）→ 抬到 3.9；且粒子播一轮就没了 → 每 1.1s 周期重踢保持常亮（竞品 yun 圈是循环的）~~
- ~~**帮工太快像滑行**：速度系数 0.7→`Game.helperSpeedScale`=0.45（竞品帮手 200/主角 400=0.5）。实测 4.3u/s~~
- ~~**烤炉一次一块太磨蹭**：`ProcessLine.cookBatch`=3，一批并排进炉同烤同出。实测 6 块 2 秒烤完~~
- ~~**收集改踩贴**（用户拍板，覆盖竞品的地面肉磁吸）：必须站上拾肉贴才吸，站贴后按节拍逐块吸整堆（竞品 DiTie_shiqu 式,不做单块距离判定）。坑：砍鱼贴离拾肉贴仅 1.4u,Plate.contains 半径太宽会边砍边吸,拾肉用专用严半径 1.1u。验证：砍鱼时肉堆地上不进包、站贴 2s 吸光~~
- ~~**背包堆叠改单列**（竞品样式）：肉/熟肉 (0, 0.09n)、金币 (0, 0.12n)，不再三列捆扎~~
- 探针教训：跳步测试（直接 onPlateUnlocked）会破坏引导链使功能贴不激活——验证引导相关功能必须走正常触发序

**第十八轮（对照竞品规格补齐游戏逻辑）：**
- ~~**开场运镜**（竞品 StartYD 0/1）：相机开局停在拦鱼区看鱼 2s（`Game.introCamHold` 旋钮，0=关），再平滑 lerp 回主角；玩家提前拖动立即结束驻留。动态采样验证：驻留期纹丝不动→归零后平滑回位~~
- ~~**引导箭头改道**（竞品 showZhiyin）：引导指着「花钱解锁」贴但金币不够时，箭头改指能赚钱的下一步（优先级照竞品：盘里有币→捡币 / 有熟肉→卖 / 后桌有货→取 / 背包有生肉→烤 / 地上有肉→捡 / 否则→砍），钱够了自动指回。不改引导 step~~
- ~~**切割机切割节奏表现**：竞品每刀播切割动画；机器模型无动画资产，用程序化压缩弹跳（squash 0.06s/回弹 0.14s）代替~~
- ~~**鱼死亡收尾**（竞品淡出）：die 动画播 1s 后 0.3s 缩小消失，代替瞬间销毁；Animator 修正：die 播完停最后一帧不再回 idle（尸体不能站起来游泳）~~
- 对照规格确认已一致（无需改）：磁吸拾肉半径、掉肉固定槽位（用户拍板覆盖竞品随机环）、顾客走位/成交补人、砍鱼转向鱼、解锁弹出特效、传送带 0.8s 节奏、金币收支双流、按间距补鱼、待机 5s 重现拖动提示（Game.updateIdleTip 早已实现，本轮撤掉了误加的重复版）
- 教训（已犯二次）：**解读运行时数据前先定死时序基准**——探针 12s 采样时开场运镜早已完成，把稳态误读成「相机卡死」，追了三轮假 bug；hold 冻结在负值是正常语义（只在驻留分支递减）

**第十七轮（主角动画/血条/帮工AI/切割机 四连修）：**
- ~~**主角只有 idle**：PlayerController.update 每帧 play(idle/run)，把 playAttack 的挥刀动作**下一帧就切回 idle**。Animator 加 oneShot 闸（一次性动作播完前循环请求不打断，force 参数供眩晕等强切）；挥刀 2.6 倍速在一刀间隔(0.35s)内挥完（PlayerController.attackAnimSpeed 旋钮）。实测砍鱼中采样 8/8 全是挥刀~~
- ~~**血条看不见**：原来是贴地朝上的小面片（斜俯视角压成几像素+埋进鱼背）。改竖立 billboard 每帧朝相机 + 加大（4.2×0.45 fill），bloodBarHeight 3.6 高过鱼背。截图清晰可见~~
- ~~**帮工傻站**：原实现只有定时逻辑传送（moveBackToSell 飞肉），模型不动。新写 updateHelper 往返 AI：跑到后桌→拿肉顶头上→跑到售卖窗→放下→返回，run/idle 动画随动，速度=主角 70%。ProcessLine 拆出 takeBackItem/putSellItem~~
- ~~**切割机看不到**：场景里真机器节点「切肉机」(Map_1/切肉机, act=false) 从没连到解锁位——buyCutter.unlockTarget 之前指向空容器 CutterAnim。已改连真机器，解锁后三刀片机器出现在河道上方~~
- 顺手修：Animator 同一 clip 资产注册两个槽（鱼 idle/run 同为游动）时二次改名会弄丢先前注册，改为名字映射
- 验证：砍鱼挥刀连续采样 ✓、帮工轨迹往返跑动 ✓、切肉机解锁显示(meshes=1) ✓、血条 billboard ✓、零报错

**第十六轮（一条半鱼/后退/撞晕动画/脚下方块 四连修）：**
- ~~**「一条半鱼」双根因**：①场景 `FishBlock` 节点（地编阶段标记拦截点的参考鱼模型）趴在拦截位，每条真鱼拦停都和它叠合——运行时开局隐藏（Game.start walk 找名字）；②补鱼判定用 `distance` 不分方向，初始鱼在出生点「后方」距离照样超阈值，开局误补叠鱼——改为沿路径带方向投影~~
- ~~**鱼后退一段**：已放行游走的鱼还占着排队名次 rank0，拦停鱼被排成第 2 名多退一个身位；且排队允许倒退。修：放行鱼(proj>0.3)不参与排序 + 排队只前进不后退（目标在身后就地停）~~
- ~~**没有撞晕动画**：同名次 bug——拦停鱼永远轮不到 queueIndex===0，playHitBarrier 不触发（clip 本身都连着）。修后实测撞桩→膨胀→眩晕链路通，且前任被砍死后补位的新队首也演~~
- ~~**主角脚下四方贴图**：Animator 套皮作用于整个角色节点树，把整张渔夫贴图图集套到了脚下光环面片上。修：Skin.apply 跳过 `quad_` 开头的面片节点 + Animator 只套模型包装层。近照确认光环恢复青白圆弧~~
- ~~**队列排不起来（连带发现）**：北河 spawn(x=14)→拦停点(3.57) 只有 10.4u，补鱼阈值 420px=10.5u 差 0.1u 永不触发，场上恒一条鱼。**respawn 阈值应=队距**：queueGapPx=380、respawnDistPx=380——队首拦停即补第二条，第二条排队后自动停补，不叠不断流~~
- 验证：拦停 [3.6/st1, 12.6/st1] 两条排队、砍死后补位+新队首演撞桩、零报错
- 鱼道几何（实测）：北河鱼从东(x=14)游向西(x=-14)，拦停点=木桩(-0.63)+stopOffset(4.2)=3.57

**第十五轮（素材全线审计与修复）——用户报「角色/鱼/金币/肉等素材全有大问题」，审计出三个系统性根因：**
- ~~**黑底 UI 面片**（气泡/地贴显示成黑方块）：Skin.groundQuad 手拼 blendState 只写了 `blend:true` 没设混合因子 → alpha 被整个忽略，PNG 透明区画成黑色。改用 `builtin-unlit` 的 `technique:1`（transparent 通道，自带 SRC_ALPHA 混合）~~
- ~~**角色/鱼「花、脏」**：静态包与骨骼包各带一套同名贴图（T_yufu_BC / T_yu_BC / T），**md5 全不同、UV 不配套**。历史上一直给骨骼模型套 resources 里的静态版 → UV 错乱。已把骨骼版拷进 `resources/texture/skin/` 带 `_sk` 后缀，Animator 新增 `skinTex` 属性统一在 build() 后套皮（Player/Helper/CustomerTemplate 场景里已设 `T_yufu_BC_sk`；鱼由 FishLane 注入 `T_yu_BC_sk`）~~
- ~~**白模道具**（肉/金币）：上一轮误删 dropMeat/spawnCoins 里的换肤（静态 FBX 材质不带贴图，删了就是白模）。已恢复：肉=`T_yu_BC`、金币=`jinibi_2`（静态模型配静态版贴图）~~
- ~~**前后鱼观感不一**（"第一条鱼是一条半鱼"的另一半根因）：第一条鱼材质异步未就绪逃过覆盖（FBX 原生 PBR 深色），后续鱼被 reinit 套错误静态贴图 → 两条鱼长得不一样叠着游。现统一为 unlit+骨骼版贴图；Fish 的受击红闪材质改为按注入的 skinTex 惰性获取~~
- 验证：角色/鱼×3/顾客/气泡全部 `builtin-unlit+贴图`、地贴白框绿框正常、鱼 UV 正确（眼/嘴/刺位置对）、零报错
- **待美术**：`wound.png`（伤口仍是暗红色块）；`bubble_emoji`、进度条、金币位图字体仍未接（台账「待接」项）

**第十四轮（四项修正）：**
- ~~**木桩改用地编现成节点**：`Map_1/Environment/WeiQiang_3/polySurface5`（世界 -0.63,-1.22,-8.82）。开局 active=false，主角踩木桩地贴→active=true。删掉我之前造的 4 段木棍（就是那"四道黑点"）~~
- ~~**鱼卡死根因=我上轮的空间排序方向反了**：proj 是「沿游动方向已推进距离」，越大越靠前，我写成升序 → 最上游的鱼被排成队首去抢桩位、真正顶桩的鱼被要求后退，两边顶死。改降序即解~~
- ~~**主角看起来奇怪的根因=材质被覆盖**：Skin.apply 把 FBX 自带的受光 PBR 材质换成了 builtin-unlit，角色不受光像贴纸浮在场景上。改为「只给没有骨骼动画的静态模型换皮」；掉落肉/金币也不再覆盖~~
- 验证：木桩显隐正确、鱼稳定顶在 x=3.6（桩位+半鱼身）、主角材质=util/dcc/imported-metallic-roughness、零报错
- **仍待观察**：鱼距已放到 480px，若仍觉得后车鱼观感重合，调 FishLane.queueGapPx（这是唯一旋钮）

**第十三轮（六项体验问题修复）：**
- ~~**白色滤镜根因=场景雾**：线性雾 start=37 而相机距主角 62，整个玩法区都在雾里被洗成灰白。Game.disableFog 开局关掉（旋钮可关回）。关掉后画面饱和度大幅提升~~
- ~~**木桩改为「地编已有节点，初始隐藏，踩地贴显现」**：删掉扔木头机制和背包木头，改 Game.barrierNode（拖木桩容器）+ revealBarrier 逐段弹出~~
- ~~**拦截点自动贴合木桩**：FishLane.barrierNode + stopOffset(4.2≈半鱼身)，鱼头正好顶住桩；修了「容器节点原点在(0,0,0)导致拦截点算到 z=0」的坑（改取子节点实际中心）~~
- ~~**鱼卡在拦截点后方**：队位原来按数组顺序排，改成按「沿路径的空间先后」排序,不再互相抢位~~
- ~~**两条鱼半重合**：队距 380→480px、补鱼 420→520px~~
- ~~**撞桩演出**：队首鱼顶住木桩瞬间播「膨胀一次 → 眩晕循环」+ 撞击音（竞品同款两拍）~~
- ~~**砍鱼伤口**：按血量分档在鱼身留伤痕面片（最多 3 处，契约图 texture/ui/wound.png，缺图用暗红块）；血特效改打在鱼身朝主角一侧的上表面，比打在几何中心显眼~~
- 验证：雾已关、木桩踩地贴显现、鱼停在 x=4.9 顶住桩(state=Blocked)、同屏单鱼不重合、零报错
- **待美术**：`wound.png` 伤口贴图（放进 assets/resources/texture/ui/ 即自动替换暗红块）

**第十一轮（骨骼动画接入 —— 最大短板补上）：**
- 美术交付 `砍大鱼经营_Fbx/`：8 个 Skin（网格+骨骼）+ 13 个动画 clip，已入库 `assets/Res/Skinned/{Char,Fish}`
  - 三个角色共用一套骨骼（各 51 骨）→ Idle/Run/Attack 通用；刀_切割 给切割机
  - 鱼：河豚 run/die/pengzhang/yun（晕和膨胀用 Skin02）、鲨鱼 run/die、灯笼鱼 run/die、鳄鱼 run
- ~~新增 `Animator.ts`：Skin 预制体 + clip 组装成会动的角色，带 scale/pitch/yaw/offset 修正旋钮~~
- ~~接入：主角(idle/run/attack)、顾客(idle/run，模板节点 instantiate 避开 prefab create 缺陷)、帮手、鱼(游/膨胀/死)~~
- ~~有骨骼动画时自动关掉程序化颠步/摆尾，避免两套动作打架~~
- **踩了三连坑（已回写 skill）**：烘焙模式让缩放失效 → 动画覆写根节点缩放（加 ScaleWrap 隔离）→ addComponent 后 start() 抢先用默认值初始化（改「先配置后入场景树」）；外加 Fish.reinit 把缩放打回 1（基准值改惰性采集）
- 验证：鱼 7.4u 长、缩放生效、动画在播、零报错
**第十二轮（剩余素材全部接入）：**
- ~~河豚换 HeTun_Skin02（含晕/膨胀几何的超集皮），四个 clip 全接：游/膨胀/眩晕/死亡~~
- ~~Animator 增加第 5 个动作槽 stun；Fish 残血进眩晕态自动切眩晕循环，且眩晕后受击不再被膨胀打断~~
- ~~主角砍鱼接 Attack 骨骼动画（有 clip 用 clip，无则退回程序化前倾）~~
- ~~切割机接 刀_切割 动画（新建 CutterAnim 节点，解锁时揭示并循环播）~~
- ~~鳄鱼道接 EYu_Skin + EYu_run（缩放 10）~~
- 验证：8 个骨骼动画对象在跑（主角 idle/run/action、河豚 run/action/die/stun、6 顾客 idle/run），砍鱼链正常，零报错
- **剩余待调（都是数值，不是缺件）**：鱼的俯仰/朝向修正（FishLane.fishPitch / modelYawOffset）、鳄鱼缩放、顾客与帮手缩放核对
- **备用未连**：鲨鱼/灯笼鱼的 Skin+run/die（策划案要出 3 个鱼版本时，把 FishLane 的 fishPrefabA + 4 个 clip 一起换成对应鱼即可）

**第十轮（逐帧手感对齐,对照代码行为规格+PDF）：**
- ~~程序化动作补竞品帧动画：鱼游动摆尾+浮沉(每条相位错开)、主角跑动颠步、顾客行走颠步、挥刀劈砍动作(面向鱼前倾 28° 回弹,每刀触发)~~
- ~~金币从顾客位置抛物线连串喷向收银盘(0.03s/枚,弧高 300px,竞品同款),不再原地下落~~
- ~~扔木头对齐竞品节奏:0.05s/根连投+0.12s 短弧;修复由此引出的回归(并行投掷按序号定目标段,原按落地计数会多根瞄同一段导致木桩永远建不成)~~
- ~~烤后桌 3 列码放(竞品 fishpos2 同款)、烤炉烟雾粒子激活时踢发~~
- 全链验证:木桩 4s 建成、烤炉 6s 出 8 块、金币落盘 30 枚、引导至 step6,零报错
- 剩余对照项:顾客头顶 x2 气泡(需气泡美术)、伤口贴片(需美术)、帮手模型走位动画、六语 UI 图放回契约路径

**第九轮（鱼流回退/挤压/受击表现）：**
- ~~拦鱼放行修复：已越过栅栏的鱼不再被拉回队位（沿路径投影判定,过线即放行游到终点回收）；frontFish 只认「已拦停」的鱼~~
- ~~队距 380px / 补鱼 420px / 同屏 4 条,鱼不再挤成一团~~
- ~~受击表现补齐（参考口径）：整鱼变红 0.15s（tint 材质切换,共享缓存）+ 悬浮血条（深底+绿条,跟随鱼头顶,素材契约 hpbar_bg/hpbar_fill 可换皮）+ 河豚受击膨胀（已有）+ 血粒子特效补 play 踢发（kdy 预制体非自动播）+ fxScale 旋钮~~
- 验证：拦停后队首平滑就位无倒退、变红+血条截图确认、零报错
- 待逐帧对照项：血粒子在截图里不明显（可能 fxScale 尺度问题,预览里肉眼确认,不对就调 Game.fxScale）、伤口贴片(需美术)、挣扎/眩晕的帧动画(FBX 无动画)

**第八轮（七项反馈整改）：**
- ~~单鱼种连续流：三个皮全指河豚,体型 8 倍(充满河道),队距 300px/补鱼 340px~~
- ~~拦鱼线对齐地图自带栅栏 WeiQiang_3(x≈0,z=-10):FishBlock(3.5,-9),木桩段 x=0.7 纵跨河道~~
- ~~鱼肉定点码放:MeatDropCenter 3×2 六槽网格+每 6 块升层,堆空归零,不再乱飞~~
- ~~烤鱼提速:cookTime 1.2→0.35s~~
- ~~顾客队伍拉直:沿 -X 直线(QueueTail -10,-0.1;入场 -15,-0.1)~~
- ~~金币堆落地贴合拾取贴:CoinPan(-3,0.05,3.3)与 P6_pickCoin 同位,走过即吸~~
- ~~背篓通天柱修复:三列捆扎堆叠(肉 0.24×0.1 / 币 0.2×0.12)~~
- ~~资产整理:未使用的 4 个设备 FBX 副本+4 张贴图副本隔离进 assets/_未使用/;全量清单见工程根 资产清单.md~~

**第七轮（UI 层完成）：**
- ~~UI 骨架：UICanvas(Canvas+Widget, UI_2D 层) + UICamera(正交 640,只看 UI_2D,priority 100) + CoinCounter(Label+Widget 贴右上) + DragTip(Widget 贴底) + EndCard(初始隐藏)~~
- ~~HUD.ts 运行时填充视觉：金币计数(底板+图标+数字脉冲) / 拖动提示(摇杆盘+画圈手柄+「拖动移动」) / 结算页(全屏黑幕+标题+PLAY NOW 1s 脉冲+点任意处跳店)~~
- ~~换皮契约：resources/texture/ui/{coin_icon|logo|playnow}.png 同名放回即替换代码回退绘制~~
- 竖版验证截图全过,零报错
- 踩坑回写:运行时 new Node() 默认 DEFAULT 层,UI 相机看不见还会被主相机当 3D 物体渲染(黑幕悬浮在地图上)——HUD 里 markLayer 递归打 UI_2D,含异步回调新建的子节点

**第六轮补丁（模型出轴偏移复发）：**
- ~~「杆子戳着走」根因复发：FBX 预制体实例内部子节点的位置覆盖（MCP 写入场景）**不持久**,预览重载被预制体原值还原（yufu 又回到 z=4.12）。改为运行时归零：PlayerController.start / CustomerQueue.spawn / Game(帮手) / FishLane.spawnFish 统一把模型首个子节点 XZ 归零~~
- 实测四朝向网格中心偏差从 4.05 → ≤0.07,原地转向,交互点位与模型重合
- 经验:FBX 实例内部子节点改 transform 一律走运行时代码,别信场景覆盖

**第五轮（轴向总修正——鱼运动/控制/点位问题的总根源）：**
- ~~像素级地形扫描确认：**河道沿 X 轴横贯，共两条**——北河 z∈[-11,-7]（砍鱼区）、南河 z∈[7,12]（二期/鳄鱼），中间 z∈[-6.5,6.5] 是作业区。此前所有点位按「河沿 Z」摆的,全部重摆~~
- ~~鱼道改沿 X：出生 (14,-9) → 终点 (-14,-9)（画面上从右上游向左下,同效果图）；拦截点 (5,-8.2)；木桩 4 段改在 x=4.6 纵跨河道 z -10.5..-7.5~~
- ~~河道阻挡改两条 Z 带（river1/2MinZ/MaxZ,Inspector 可调）,扩建后放开~~
- ~~岸边点位重摆：放木桩贴 (6.5,-5.9)、砍鱼贴 (5,-5.9)、拾肉/掉肉中心 (3.4,-5.4)、切割机贴 (1.2,-4.6)、传送带贴 (1.4,-3.4)、玩家出生 (2.5,-4)、二期砍鱼贴 (3,6.2)、扩建贴 (-1,5.5)、鳄鱼道南河 (14,9.5)→(-14,9.5)~~
- ~~验证截图：鱼队沿河排开、队首拦在木桩前、鱼头朝游动方向（yawOffset=0 即正确）、掉肉钳在南岸~~
- ~~鱼体型旋钮：FishLane.fishScaleA/B/C（河豚2.0/灯笼鱼2.4/鲨鱼1.0 初值,按策划案「其他鱼大于河豚」再调）~~
- 经验：判断世界轴向不能靠斜视角截图目测（两种解释都能「看着像」）,要用行/列扫描的整行同色模式或多点已知坐标标记

**第四轮（对照代码行为规格逐项对齐玩法）：**
- ~~引导第 2 步改为「第一块肉落地」触发（参考行为,不等鱼死）~~
- ~~顾客补位改「成交后 0.5s 补人」（参考：补人只发生在成交时,按间距补人是死代码）~~
- ~~帮手解锁后自动搬后桌熟肉上货（0.5s/件,HELPER_WORK_INTERVAL）~~
- ~~传送带解锁后每 0.8s 自动吸地面肉上带进炉（BELT_FEED_INTERVAL）~~
- ~~切割机伤害对齐参考 10/循环+3 肉（CUTTER_DAMAGE）~~
- ~~拦停头鱼头顶挂眩晕特效（kdy_yun,鱼死随节点销毁）~~
- ~~首屏运镜：相机从拦鱼区起步,跟随 lerp ≈2s 拉回主角（参考:先看鱼再回主角）~~
- 探针验证:砍数刀后 step 已到 3（肉落地→亮拾取→背上有肉→亮烤炉贴）,零报错
- 已知与参考的有意差异:切割机产肉进地面（参考进专用肉盘 meatPanList 6列）、木桩 4 段（参考 9 根,按我们河宽定）、挥刀/膨胀动画用缩放 tween 顶（FBX 无动画子资产）

**第三轮（2026-07-29 晚，按 PDF 设计文档+新素材整改）：**
- ~~鱼运动根因修复：鱼过终点不回收、一直游出地图 → 改为过终点回收+上游补鱼,形成均匀鱼流（间距旋钮 FishLane.respawnDistPx=260px,实测 4 条等距循环、木桩建成后整队拦停在桩前）~~
- ~~全部模型运行时换皮（Skin.ts,按贴图使用说明：渔夫/顾客/帮手=T_yufu_BC,鱼/肉=T_yu_BC,金币=jinibi_2,木头=T）~~
- ~~地贴可视化：白/绿双态底框+业务图标,踩上变绿。素材路径契约 resources/texture/ui/plate_*.png,缺图回退色块（原资源图目录已被移走,美术图放回同名路径即自动换皮）~~
- ~~引导箭头可视化（缺图回退青色三角）~~
- ~~kdy_fx 特效包入库（assets/jiu + assets/Res/fx,注意 res/Res 大小写合并）并接线：砍中喷血 kdy_xue、解锁 kdy_shengji、烤炉烟雾 kdy_yanwu、眩晕 kdy_yun（已连,眩晕表现待接 Fish）~~
- ~~掉肉落点钳制在岸上（不再掉进河里）~~
- 设计分辨率 720×1280 已生效（编辑器重启后读取）
- 素材现状：UI 六语图（logo/按钮/提示/摇杆/地贴）原在 砍大鱼资源-已压缩/2.资源图,该目录已从桌面移走——需要把这些图按 `assets/resources/texture/ui/` 契约名放回（映射见 Plate.ts iconMap 注释）

**第二轮修正（2026-07-29 下午，针对「人物摆放/鱼运动不对」）：**
- ~~量了 Map_1 真实几何（运行时 dump）：河道沿世界 Z 轴 x≈8.5（画面斜是相机 yaw −48.6°）、围栏区 x∈[−4.2,4.7]、东列=加工链、西列桌=贩卖/收银、北带=砍鱼作业区~~
- ~~按效果图分区重摆全部锚点/地贴/盘位（之前是瞎猜的）~~
- ~~拖动改相机相对映射（之前差 45°），8 方向标定实测方向/速度全对~~
- ~~禁用 Main Camera-001（之前双相机同时启用，渲染的和跟随的不是同一台）~~
- ~~背篓改双栈（生肉/熟肉分开，对齐参考的 fishMeatIn/kaoyuIn）~~
- ~~开局木头数自动=木桩段数；加河道阻挡（扩建后放开过河）~~
- ~~全链条探针验证通过：木桩拦鱼→60刀砍死→掉肉→拾肉→烤炉→取烤肉→上架→顾客买→金币→拾币→解锁贴亮,零报错~~

**⚠️ 唯一待用户操作**：设计分辨率还是 1280×720 横版。我已把 720×1280 写进 `settings/v2/packages/project.json`，但开着的编辑器不热加载——请在 **项目设置→项目数据** 里改成 720×1280（fitWidth），或重启编辑器。改完后编辑器预览的竖屏方向才正确。

**已完成（首轮会话）：**
- ~~解包参考 index(3).html（108图/26音频/逻辑源码/场景树坐标全部还原）~~ → `../参考解包_index3/`
- ~~玩法与数值分析（三方互校：代码实测=策划案）~~
- ~~17 个 FBX + 4 张贴图导入 `assets/Res/Roles/`~~
- ~~玩法脚本 11 个（`assets/Scripts/`，原创实现）：GameConfig / FlyUtil / Backpack / Fish / FishLane / PlayerController / Plate / Customer / CustomerQueue / ProcessLine / GuideArrow / Game~~
- ~~Map_1 场景搭建：GameRoot（Points 锚点 8 个 / 12 块地贴挂 Plate / 木桩 4 段 / Player+渔夫模型 / 双鱼道 / 顾客队列 / 加工线 4 盘位 / 帮手隐藏位 / 引导箭头）全部 MCP 连线完成~~
- ~~无头探针验证：拖动移动✓（恒速+边界钳制✓）鱼群游动/补鱼✓ 木头交付→木桩点亮→鱼道封锁→引导进砍鱼步✓ 控制台零报错~~

**已知坑（本会话踩过，已回写 skill）：**
- MCP `set_component_property` 不支持数组属性 → 脚本已按「容器子节点/多单属性」规避
- FBX 用 assetPath 实例化假成功 → 必须用 prefab 子资产 uuid（`<uuid>@xxxxx`，从 .meta subMetas 读）
- 空节点 initialTransform 丢 Z → 一律 `node_transform customProperties` 补写

**下一步（按批次）：**
1. [ ] **材质贴图**：FBX 目前灰模。按 `模型资产包/贴图使用说明.txt` 建 4 个材质（渔夫系 T_yufu_BC / 鱼系 T_yu_BC / 金币 jinibi_2 / 通用 T）并指给各 Mesh
2. [ ] **走完整条链的闭环验证**：砍鱼掉肉→拾肉→烤炉→贩卖→金币→解锁×4→扩建→鳄鱼→胜利（探针闭环脚本雏形在会话 scratchpad：drive_preview.js 思路）
3. [ ] **UI 层**：金币计数（右上角）/ DRAG TO MOVE 引导（素材在 `砍大鱼资源-已压缩/2.资源图/`，6语）/ 结算页 PLAY NOW
4. [ ] **帮手 AI**：解锁后自动取熟肉→贩卖（参考 Pawn_userplayer 行为，见代码行为规格.md）
5. [ ] **表现**：挥刀动画（FBX 无动画，用代码 tween 或后补骨骼动画）、鱼受击膨胀仅河豚、烤烟粒子（kdy_fx.zip 有现成 prefab：yanwu/yun/xue/shengji）、音效
6. [ ] **手感标定**：PX_PER_METER=40 初值（GameConfig.ts:8），对照参考录屏调移速/距离手感
7. [ ] **多语言 + SDK 对接 + 压包**（A线交付流程）

## 可编辑面（在编辑器里拖，不用改代码）

| 什么 | 在哪 |
|---|---|
| 所有触发点位 | `GameRoot/Points/*`（鱼出生/终点/拦截、掉肉中心、顾客入场/队首/队尾、鳄鱼两点） |
| 12 块地贴位置 | `GameRoot/Plates/P0_wood … P11_chop2`（命名序号=引导链顺序，Game 按序号解析） |
| 地贴类型/半径/价格 | 各地贴 Plate 组件：kind / radiusOverridePx / needCoinOverride / unlockTarget |
| 烤炉四盘位 | `GameRoot/ProcessLine/{FrontPan,OvenSlot,BackPan,SellPan}` |
| 背篓/金币挂点 | `GameRoot/Player/{BagPoint,CoinPoint}` |
| 移动边界/摇杆 | Player 的 PlayerController：boundMin/Max、stickRadius、轴向系数 |
| 全部数值 | `assets/Scripts/GameConfig.ts`（速度/血量/间隔/距离/容量/价格,注释齐全） |

## 回退

- 脚本层：`assets/Scripts/` 全部为本会话新增，删除即回退
- 场景层：Map_1 新增节点都在 `GameRoot` 一个根下；另有三处改动：切肉机/传送带设为隐藏（解锁揭示）、无其他既有节点改动。回退 = 删 GameRoot + 把切肉机/传送带 active 勾回
- 新增资源：`assets/Res/Roles/`（FBX+贴图拷贝），删除即回退
