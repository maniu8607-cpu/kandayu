# 美术素材清单（谁在用什么 · 2026-07-29 晚更新）

> 判断口径：`resources/` 下按**路径**被代码引用的，和场景/组件按 **uuid** 引用的，才算「在用」。
> 没用上的统一进 `assets/_未使用/`（隔离不删，确认后你在编辑器右键删）。
> **换皮契约**：下表「契约路径」里的文件，同名覆盖即生效，不用改代码、不用重新连线。

---

## 一、UI 图（`assets/resources/texture/ui/`）— 来自「砍大鱼资源-已压缩/2.资源图」，46 张全部在用

### 语言无关（28 张）

| 契约文件名 | 原始文件 | 用在哪 |
|---|---|---|
| `coin_bar` | 1.货币栏 | 右上角金币计数底板 |
| `plate_small_white` / `plate_small_green` | 6 / 5.地贴-小框 | 站立·交付·拾取类地贴的底框（白=常态，绿=踩上） |
| `plate_mid_white` / `plate_mid_green` | 12 / 13.地贴-中框 | 购买类地贴（帮手/切割机/传送带/扩建）底框 |
| `plate_footprint` | 3.地贴-脚印 | 砍鱼贴图标 |
| `plate_cook` | 4.地贴-烤鱼肉 | 烤炉交付 / 取烤肉贴图标 |
| `plate_storage` | 7.地贴-置物区域 | 放木桩 / 拾肉 / 贩卖贴图标 |
| `plate_knife` | 8.地贴-菜刀 | 切割机购买贴图标 |
| `plate_expand` | 9.地贴-扩张 | 扩建贴图标 |
| `plate_cashier` | 10.地贴-收银员 | 帮手购买贴图标 |
| `plate_belt` | 11.地贴-传送带 | 传送带购买贴图标 |
| `arrow_tri` | 14.指引-三角形 | 引导箭头链（主角→目标，32px 一枚） |
| `foot_ring` | 15.指引-脚下圆圈 | 主角脚下光环（竞品同款站位指示） |
| `stick_base` / `stick_ring` / `stick_knob` | 18 / 17 / 16.摇杆 | 拖动提示的摇杆三件套 |
| `bubble_x2` / `bubble_x1` / `bubble_x0` | 27 / 26 / 25.气泡-顾客 | 顾客头顶购买数气泡（买一份切一张） |
| `bubble_emoji` | 28.气泡-顾客-表情 | 顾客买完的满足表情（**待接**） |
| `hpbar_bg` / `hpbar_fill` | 30 / 31.血条 | 大鱼头顶血条 |
| `progress_bg` / `progress_fill` / `progress_cap` | 32 / 33 / 34.进度条 | 加载页进度条（**待接**） |
| `tip_small` | 29.提示 | 小提示（**待接**） |
| `wood_empty` | 2.河道木桩空状态 | 2D 木桩空位图（我们木桩是 3D 模型，**当前未用**） |

### 六语（每语 3 张 × 6 = 18 张）`texture/ui/<lang>/`

| 契约文件名 | 原始 | 用在哪 |
|---|---|---|
| `logo` | 41~46.logo-各语言 | 结算页标题 |
| `playnow` | 35~40.按钮-各语言 | 结算页 PLAY NOW 按钮 |
| `tip_drag` | 19~24.提示-各语言 | 开局「拖动移动」引导条 |

语言目录：`zh` 简体 / `zh-tw` 繁体 / `en` 英文 / `ja` 日语 / `ko` 韩语 / `vi` 越南语。
运行时按浏览器语言自动选，投放时可用 `Lang.force = 'ja'` 写死。

### 位图字体 `assets/resources/font/`
`@CountNumFont-export.fnt` + `.png`（**已修**：fnt 内原本写着 Windows 绝对路径导致贴图找不到）。**待接**：金币数字换成这套字体。

---

## 二、模型（`assets/Res/Roles/Mesh/`）— 来自「砍大鱼模型资产包」

| 在用 | 说明 |
|---|---|
| 渔夫 / 顾客 / 售卖员 | 角色，套 `T_yufu_BC` 皮 |
| 河豚 | 当前唯一在用的鱼（三个鱼皮槽都指它） |
| 木棍 / 金币 / 熟肉 / 河豚肉 | 道具 |
| 鳄鱼 | 终局展示 |
| 鲨鱼 / 灯笼鱼 / 鲨鱼肉 / 灯笼鱼肉 | **暂不用但保留**——策划案要出 3 个鱼版本，换版本时把 FishLane 的 `fishPrefabA` 指过去即可 |

贴图 `assets/resources/texture/skin/`：`T_yufu_BC`(角色) / `T_yu_BC`(鱼和肉) / `jinibi_2`(金币) / `T`(木头等)，运行时按《贴图使用说明》套皮。

> **⚠️ 静态版与骨骼版是两套 UV，同名不同图（md5 全不同），绝不能混用**：
> - `T_yufu_BC_sk` / `T_yu_BC_sk` / `T_sk` —— 从 `Res/Skinned/` 拷来的**骨骼版**，给骨骼模型（渔夫_Skin/阿牛_Skin/HeTun_Skin 等，走 Animator.skinTex）
> - 不带后缀的原三张 —— **静态版**，给静态模型（河豚肉/金币/熟肉/顾客.fbx 等）
> 混套的表现是模型「花的、脏的」（眼睛嘴巴错位成杂色块）。

---

## 三、特效（`assets/jiu/prefab/` + `assets/Res/fx/`）— 来自 kdy_fx

`kdy_xue` 砍中血 / `kdy_yun` 眩晕星 / `kdy_yanwu` 烤炉烟 / `kdy_shengji` 解锁升级——全部已连到 Game 组件。粒子非自动播，代码里统一踢 `play()`。

---

## 四、音效（`assets/resources/audio/`）— 来自「音效」文件夹，12 个全部在用

bgm / fish_swim / hit_wall / chop / wood_throw / pickup / oven / belt / unlock_device / unlock_area / croc / win

---

## 五、已隔离（`assets/_未使用/`）

| 项 | 为什么 |
|---|---|
| 传送带 / 切肉机 / 烤肉台 / 桌子 .fbx | 与 `Res/Environment/Mesh/` 里你已摆进场景的同名模型重复（早期误导入副本） |
| Roles_Tex_已入resources/ | 贴图原位副本，运行时读的是 resources 里那份 |

---

## 六、还缺的美术（代码已留接口，给图即生效）

| 缺什么 | 影响 |
|---|---|
| **角色/鱼的骨骼动画** | 最大短板：跑、砍、游、膨胀全靠程序化模拟，是与竞品手感差距的主因 |
| 鱼身伤口贴片 | 竞品砍到一定血量会显示刀痕（sk1/sk2/sk3 递进） |
| 加载页 logo 布局 | 进度条素材有了，加载页还没搭 |

## 源目录（工程外，不打包）
`../砍大鱼资源-已压缩/`（UI 原图 + 效果图 + fnt）、`../砍大鱼模型资产包/`（FBX+贴图）、`../音效/`、`../kdy_fx/`、`../参考解包_index3/`（竞品分析产物）
